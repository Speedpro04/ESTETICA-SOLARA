node_modules
dist
.git
.github
.claude

# Contém SERVICE_ROLE em texto puro. Fora do contexto de build: mesmo não indo
# para a imagem final, entraria na camada intermediária e no cache.
_original

# Segredos e configuração local. O build lê .env.production, que é versionado
# e só tem valor público.
.env
.env.local
.env.*.local
backend/.env

# Backend Python: imagem separada.
backend

# Arte original da logo. A versão otimizada já vive em public/.
LOGO-ESTETICA-7777

# scripts/ NÃO entra aqui: o `npm run build` chama scripts/sitemap.mjs. Os
# outros scripts da pasta (geração de logo e imagens) só rodam na máquina, mas
# são poucos KB e não vale separar em duas pastas para economizar isso.

*.log
